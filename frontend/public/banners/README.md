Taruh gambar banner promosi di folder ini (misal `promo-1.jpg`),
lalu daftarkan path-nya di array `BANNERS` pada
`src/presentation/components/PromoBanner.jsx`, contoh:

```js
const BANNERS = ['/banners/promo-1.jpg', '/banners/promo-2.jpg'];
```
